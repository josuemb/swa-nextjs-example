{
  "name": "api",
  "version": "1.0.0",
  "license" : "SEE LICENSE IN README.MD",
  "description": "REST API using Azure Functions and Typescript as language.",
  "scripts": {
    "prebuild": "yarn install",
    "build": "tsc",
    "watch": "tsc -w",
    "prestart": "yarn install && yarn build",
    "start": "func start --typescript",
    "test": "echo \"No tests yet...\""
  },
  "dependencies": {},
  "devDependencies": {
    "@azure/functions": "^1.2.3",
    "typescript": "^4.0.0"
  }
}
